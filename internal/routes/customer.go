package routes

import (
	"crm_lite/internal/core/resource"

	"github.com/gin-gonic/gin"
)

// registerCustomerRoutes 注册客户模块路由 (待实现)
func registerCustomerRoutes(rg *gin.RouterGroup, resManager *resource.Manager) {
	// customerController := controller.NewCustomerController(resManager)
	// customer := rg.Group("/customers")
	// {
	// 	customer.GET("/", customerController.List)
	// }
}
